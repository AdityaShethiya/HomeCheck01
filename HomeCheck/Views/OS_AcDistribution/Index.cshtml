@model IEnumerable<HomeCheck.Models.OS_AcDistribution>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.acDistribution)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.acDistribution)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.acDistributionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.acDistributionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.acDistributionID })
        </td>
    </tr>
}

</table>
