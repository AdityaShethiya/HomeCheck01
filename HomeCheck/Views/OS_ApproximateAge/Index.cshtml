@model IEnumerable<HomeCheck.Models.OS_ApproximateAge>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.approximateAge)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.approximateAge)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.approximateAgeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.approximateAgeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.approximateAgeID })
        </td>
    </tr>
}

</table>
