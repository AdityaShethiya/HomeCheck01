@model IEnumerable<HomeCheck.Models.OS_AdditionalBuildings>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.additionalBuilding)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.additionalBuilding)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.additionalBuildingID }) |
            @Html.ActionLink("Details", "Details", new { id=item.additionalBuildingID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.additionalBuildingID })
        </td>
    </tr>
}

</table>
