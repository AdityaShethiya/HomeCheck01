@model IEnumerable<HomeCheck.Models.OS_AluminiumWiring>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.aluminiumWiring)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.aluminiumWiring)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.aluminiumWiringID }) |
            @Html.ActionLink("Details", "Details", new { id=item.aluminiumWiringID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.aluminiumWiringID })
        </td>
    </tr>
}

</table>
