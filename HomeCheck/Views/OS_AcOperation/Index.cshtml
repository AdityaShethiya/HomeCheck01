@model IEnumerable<HomeCheck.Models.OS_AcOperation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.acOperation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.acOperation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.acOperationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.acOperationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.acOperationID })
        </td>
    </tr>
}

</table>
