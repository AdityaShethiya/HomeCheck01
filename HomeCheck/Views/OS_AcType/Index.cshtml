@model IEnumerable<HomeCheck.Models.OS_AcType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.acType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.acType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.acTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.acTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.acTypeID })
        </td>
    </tr>
}

</table>
